# Contributing

Keep changes small enough to review and explain.

For architecture changes, add or update an ADR under `docs/decisions/` when the trade-off is not obvious from the code.

Before opening a pull request:

```bash
pip install -e .[dev]
pytest
```

Pull requests should explain the problem being solved, the chosen approach, important trade-offs and any follow-up work that remains.
